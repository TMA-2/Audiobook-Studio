# Generation Replace Fix Plan

Summary of changes made to fix the uncaught replace error on undefined properties in App.tsx and SettingsDialogue.tsx.

## Fixed
- Added fallback defense `(gen.model || 'unknown')` before invoking `.replace('-tts', '')` on line 1757 in `src/App.tsx` to handle legacy generation items gracefully.
- Added fallback defense `(project.title || 'project')` before invoking `.replace` for export file name sanitization in `src/App.tsx`.
- Added fallback defense `(tempPromptTemplate || '')` before invoking `.replace` when compiling the assembled prompt layout preview inside the Settings Dialogue.
- Integrated a state synchronization `useEffect` inside `SettingsDialogue.tsx` to ensure all temporary inputs, character models, speaker specifications, and custom prompt templates refresh gracefully and match the parent project state whenever the hub is displayed.
